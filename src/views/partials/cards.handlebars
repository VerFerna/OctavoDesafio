<div style="display: flex; flex-wrap: wrap; justify-content: center; width: 100%" id="list">
    {{#each products}}
    {{#if status}}
    <div class="card" style="margin: 20px 100px; max-width: 200px">
        {{#if thumbnail}}
        <img src={{thumbnail}} width="200px" alt="img" >
        {{else}}
        <img src="https://i.ibb.co/zsQdBNc/200x200.gif" width="200px" alt="img - no img">
        {{/if}}
        <div class="card-body">
            <p class="card-title">{{category}} - {{title}}</p>
        </div>
        <div class="p-3" style="display: flex; justify-content: space-between;">
            <a href="/products/{{id}}" class="btn btn-primary">Info</a>
            <div class="delete">
                <button class="btn btn-primary delete" id={{id}}>
                    Delete
                </button>
            </div>
        </div>
    </div>
    {{/if}}
    {{/each}}
</div>